# Web framework
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5
requests==2.31.0
Pillow>=11.0.0  # Updated for Python 3.13 compatibility
python-dotenv==1.0.0

# Database
redis==5.0.1

# MCP Integration
mcp>=1.0.0  # Updated to latest stable version

# AutoGen (Multi-agent framework) - Version 0.7.5
autogen-agentchat==0.7.5
autogen-ext[openai]==0.7.5

# Utilities
pydantic>=2.10.0  # Updated for Python 3.13 compatibility (pre-built wheels available)

# Core ML libraries (install PyTorch separately with CUDA)
# torch - install separately: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# torchvision - install with torch
# torchaudio - install with torch

# Image generation dependencies
diffusers>=0.24.0
transformers>=4.36.0
accelerate>=0.24.0
safetensors>=0.4.0

# Performance optimization (install after PyTorch)
# xformers - install separately: pip install xformers